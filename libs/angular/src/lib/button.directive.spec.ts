import { ButtonDirective } from './button.directive';

describe('MlButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDirective();
    expect(directive).toBeTruthy();
  });
});
