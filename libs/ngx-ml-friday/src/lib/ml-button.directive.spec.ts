import { MlButtonDirective } from './ml-button.directive';

describe('MlButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MlButtonDirective();
    expect(directive).toBeTruthy();
  });
});
